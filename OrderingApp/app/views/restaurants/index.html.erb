<div>

	Restaurants:
	<ul>
		<%= @restaurants.each do |r| %>
		<div>
			<%= r.id %>
			<%= link_to 'Edit', edit_restaurant_path(r)%>
		</div>
		<% end %>
	</ul>

</div>

<%= link_to 'New restaurant', new_restaurant_path %>
